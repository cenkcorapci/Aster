# Switches to aspect_rules_js/ts + npm packaging in milestone M6
# (docs/development-plan.md). Package: @aster-db/client.

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "srcs",
    srcs = glob(["src/**/*.ts"]) + ["package.json"],
)
