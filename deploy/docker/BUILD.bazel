load("@rules_cc//cc:cc_binary.bzl", "cc_binary")

package(default_visibility = ["//visibility:public"])

# Tiny static helper shipped in the BusyBox image (see scripts/docker-build.sh).
cc_binary(
    name = "su-exec",
    srcs = ["su-exec.cc"],
)
